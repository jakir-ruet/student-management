### Why these models? Each model directly maps to a database table

| Table           | Model          |
| --------------- | -------------- |
| `USERS`         | `User`         |
| `ROLES`         | `Role`         |
| `PERMISSIONS`   | `Permission`   |
| `LOGIN_HISTORY` | `LoginHistory` |
